package lecturecode.week4;

public class ArraysVsList {

    /*
    // properties of arrays
    - older data structure
    -memory efficient
    - we have to specify which type of data is stores in the array
    - fixed size, and you must choose size when you create them

    // properties of mutable lists
    - old data structure
    - depending on the type of list, varying levels of memory efficient for storage and varying level of speed to access and change data
    -can loop over a list
    - the data is kept in order, elements , indexed staring from 0
     */
}
