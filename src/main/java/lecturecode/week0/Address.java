package lecturecode.week0;

public class Address {
    public static void main(String[] args) {

        // declares variable for the city and state
        String city = "Minneapolis";
        String state = "mn";

     // Create a new String variable "Minneapolis,MN"
        // converts the state abbreviation to uppercase
        state.toUpperCase();

        // create a new string variable
        System.out.println(city + "," + state.toUpperCase());
    }
}
