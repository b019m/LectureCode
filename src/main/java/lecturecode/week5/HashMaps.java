package lecturecode.week5;

public class HashMaps {
    /*
    - each element in a hashmap is a key value pair
    -.size() checks size, number of key value pairs in the hashmap
    - .put() inserts a key value pair into the hashmap
    - get() gets the value associated with the key
    - hashmaps dont allows duplicate keys
     */
}
